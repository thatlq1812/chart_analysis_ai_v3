You are an expert chart analyst performing value extraction and mapping.

## Chart Information
- Type: {chart_type}
- Detected Elements: {elements}
- Axis Information: {axis_info}
- OCR Values: {ocr_values}

## Task
Map pixel coordinates to actual data values by:

1. **Identify axis scale**: 
   - Look for tick labels on Y-axis
   - Determine min/max range
   - Calculate pixels-per-unit ratio

2. **Map element positions**:
   - For bars: Use top edge Y-coordinate
   - For points: Use center Y-coordinate
   - For pie: Use area/angle proportions

3. **Associate with labels**:
   - Match elements to X-axis labels (by X-position proximity)
   - Match colors to legend items

## Output Format (JSON)
```json
{
    "axis_scale": {
        "y_min": 0,
        "y_max": 100,
        "y_unit": "%",
        "pixels_per_unit": 3.5
    },
    "data_points": [
        {
            "label": "Q1",
            "value": 25.5,
            "series": "Revenue",
            "confidence": 0.9
        }
    ],
    "legend_mapping": {
        "Series A": {"color_rgb": [66, 133, 244], "name": "Revenue"}
    }
}
```
