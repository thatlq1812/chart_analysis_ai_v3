"""
Stage 3 Extraction Unit Tests

Tests for all Stage 3 submodules:
- Preprocessor (negative image, thresholding)
- Skeletonizer (Lee algorithm, keypoint detection)
- Vectorizer (RDP algorithm, subpixel refinement)
- OCR Engine (text extraction, role classification)
- Geometric Mapper (axis calibration, value mapping)
- Element Detector (bars, markers, pie slices)
- Classifier (chart type classification)
"""
