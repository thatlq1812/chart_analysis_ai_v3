% TikZ: Stage 3 Extraction - VLM Pipeline (v6.0.0)
% Usage: \input{figures/tikz/tikz_stage3_submodules.tex}
% Requires: \usepackage{tikz} \usetikzlibrary{shapes.geometric, arrows.meta, positioning, fit, backgrounds}

\begin{tikzpicture}[
    node distance=0.4cm and 0.3cm,
    module/.style={
        rectangle, rounded corners=2pt, draw=black!60, thick,
        minimum width=2.6cm, minimum height=0.9cm,
        fill=#1!15, font=\scriptsize\bfseries, align=center
    },
    arrow/.style={-{Stealth[length=2.5mm]}, thick, draw=black!50},
    label/.style={font=\tiny\itshape, text=black!50, align=center},
    io/.style={
        rectangle, rounded corners=2pt, draw=black!40,
        minimum width=3.2cm, minimum height=0.6cm,
        fill=black!5, font=\scriptsize, align=center
    },
    decision/.style={
        diamond, draw=black!60, thick, fill=yellow!15,
        font=\scriptsize\bfseries, align=center,
        minimum width=2.2cm, minimum height=0.8cm
    }
]

% Input
\node[io] (input) {Cropped Chart Image};

% Step 1: EfficientNet-B0 Classifier
\node[module=blue, below=0.5cm of input] (cls) {EfficientNet-B0\\Classifier};
\node[label, right=0.1cm of cls] {3-class: bar / line / pie\\97.54\% accuracy};

% Step 2: Backend Selection
\node[decision, below=0.6cm of cls] (sel) {Backend\\Selection};
\node[label, right=0.1cm of sel] {pipeline.yaml:\\backend: deplot};

% Step 3: VLM Extractors (parallel options)
\node[module=teal, below left=0.7cm and 1.8cm of sel] (dep) {DePlot};
\node[label, below=0.05cm of dep] {google/deplot\\(default)};

\node[module=teal, below left=0.7cm and 0.0cm of sel] (mat) {MatCha};
\node[label, below=0.05cm of mat] {google/matcha\\(ablation)};

\node[module=orange, below right=0.7cm and 0.0cm of sel] (p2s) {Pix2Struct};
\node[label, below=0.05cm of p2s] {pix2struct-base\\(baseline)};

\node[module=purple, below right=0.7cm and 1.8cm of sel] (svlm) {SVLM};
\node[label, below=0.05cm of svlm] {Qwen2-VL-2B\\(multilingual)};

% Step 4: TableData parsing
\node[module=blue, below=1.4cm of sel] (parse) {TableData Parser};
\node[label, right=0.1cm of parse] {headers / rows / records\\linearized table -> struct};

% Output
\node[io, below=0.6cm of parse] (output) {RawMetadata + TableData (Stage3Output)};

% Arrows
\draw[arrow] (input) -- (cls);
\draw[arrow] (cls) -- (sel);
\draw[arrow] (sel) -| (dep);
\draw[arrow] (sel) -| (mat);
\draw[arrow] (sel) -| (p2s);
\draw[arrow] (sel) -| (svlm);
\draw[arrow] (dep) |- (parse);
\draw[arrow] (mat) |- (parse);
\draw[arrow] (p2s) |- (parse);
\draw[arrow] (svlm) |- (parse);
\draw[arrow] (parse) -- (output);

\end{tikzpicture}
